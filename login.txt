CREATE TABLE login (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

INSERT INTO users (name, email, password)
VALUES 
('Admin User', 'admin@example.com', 'admin123'),
('Regular User', 'user@example.com', 'user123');

SELECT * FROM login;
-- tasks table
DROP TABLE IF EXISTS tasks CASCADE;

CREATE TABLE tasks (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    priority VARCHAR(20) CHECK (priority IN ('Low', 'Medium', 'High')) NOT NULL,
    status VARCHAR(20) CHECK (status IN ('Pending', 'In Progress', 'Completed')) NOT NULL DEFAULT 'Pending',
    due_date DATE,
    assigned_to INTEGER REFERENCES login(id) ON DELETE SET NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
SELECT * FROM tasks;